import React from 'react';

const Slider = () => {
  return (
    <SliderStyles>
      <div className="section-center"></div>
    </SliderStyles>
  );
};

const SliderStyles = styled.article``;

export default Slider;
